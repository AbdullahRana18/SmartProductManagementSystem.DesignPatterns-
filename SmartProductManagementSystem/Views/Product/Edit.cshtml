@model SmartProductManagementSystem.Models.Product

@{
    ViewData["Title"] = "Edit Product";
}

<div class="container py-3 py-md-5">
    <div class="row justify-content-center">
        <div class="col-11 col-sm-10 col-md-8 col-lg-6">

            <div class="card border-0 shadow-lg rounded-4 overflow-hidden">

                <div class="card-header bg-primary text-white text-center py-4 border-0">
                    <div class="icon-circle bg-white bg-opacity-10 mx-auto mb-2 text-white">
                        <i class="bi bi-pencil-square fs-3"></i>
                    </div>
                    <h4 class="fw-bold mb-1 fs-4 fs-md-3">Edit Product Details</h4>
                    <p class="text-white-50 small mb-0">Update inventory information</p>
                </div>

                <div class="card-body p-3 p-md-5 bg-white">
                    <form asp-action="Edit" method="post">
                        <input type="hidden" asp-for="Id" />

                        <div class="mb-4">
                            <label class="form-label small text-muted fw-bold ls-1">PRODUCT NAME</label>
                            <div class="input-group input-group-lg shadow-sm rounded-3 overflow-hidden border">
                                <span class="input-group-text bg-white border-0 text-muted ps-3 d-none d-sm-flex">
                                    <i class="bi bi-tag"></i>
                                </span>
                                <input asp-for="Name" class="form-control border-0 shadow-none ps-3 ps-sm-2 fs-6" placeholder="Product Name" />
                            </div>
                            <span asp-validation-for="Name" class="text-danger small ms-1"></span>
                        </div>

                        <div class="row g-3 mb-4">
                            <div class="col-12 col-md-6">
                                <label class="form-label small text-muted fw-bold ls-1">PRICE (PKR)</label>
                                <div class="input-group shadow-sm rounded-3 overflow-hidden border">
                                    <span class="input-group-text bg-white border-0 text-muted ps-3">
                                        <i class="bi bi-currency-dollar"></i>
                                    </span>
                                    <input asp-for="Price" class="form-control border-0 shadow-none ps-2 fs-6" />
                                </div>
                                <span asp-validation-for="Price" class="text-danger small ms-1"></span>
                            </div>

                            <div class="col-12 col-md-6">
                                <label class="form-label small text-muted fw-bold ls-1">STOCK QUANTITY</label>
                                <div class="input-group shadow-sm rounded-3 overflow-hidden border">
                                    <span class="input-group-text bg-white border-0 text-muted ps-3">
                                        <i class="bi bi-layers"></i>
                                    </span>
                                    <input asp-for="StockQuantity" class="form-control border-0 shadow-none ps-2 fs-6" />
                                </div>
                                <span asp-validation-for="StockQuantity" class="text-danger small ms-1"></span>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label small text-muted fw-bold ls-1">CATEGORY</label>
                            <div class="input-group shadow-sm rounded-3 overflow-hidden border">
                                <span class="input-group-text bg-white border-0 text-muted ps-3">
                                    <i class="bi bi-folder2-open"></i>
                                </span>
                                <select asp-for="CategoryId" class="form-select border-0 shadow-none ps-2 fs-6" asp-items="ViewBag.Categories">
                                </select>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label small text-muted fw-bold ls-1">PRODUCT TYPE (Fixed)</label>
                            <div class="input-group shadow-sm rounded-3 overflow-hidden border bg-light">
                                <span class="input-group-text bg-transparent border-0 text-muted ps-3">
                                    <i class="bi bi-info-circle"></i>
                                </span>
                                <input asp-for="ProductType" class="form-control border-0 shadow-none ps-2 bg-transparent text-muted fs-6" readonly />
                            </div>
                        </div>

                        <div class="d-grid gap-2 pt-2">
                            <button type="submit" class="btn btn-primary btn-lg rounded-3 shadow-sm fw-bold py-2 py-md-3 fs-6">
                                <i class="bi bi-check2-all me-2"></i> Save Changes
                            </button>
                            <a asp-action="Index" class="btn btn-light btn-lg rounded-3 text-muted py-2 py-md-3 fs-6 border">
                                Cancel
                            </a>
                        </div>

                    </form>
                </div>
            </div>

        </div>
    </div>
</div>

<style>
    body {
        background-color: #f5f7fa;
    }

    /* Icon Circle Responsive */
    .icon-circle {
        width: 45px;
        height: 45px;
        @@media (min-width: 768px) {
            width: 50px;
            height: 50px;
        }

        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        backdrop-filter: blur(5px);
    }

    /* Focus Effects (Blue for Edit) */
    .input-group:focus-within {
        box-shadow: 0 0 0 3px rgba(13, 110, 253, 0.15) !important;
        border-color: #0d6efd !important;
    }

    .ls-1 {
        letter-spacing: 1px;
    }

    .btn-primary {
        transition: all 0.2s;
    }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(13, 110, 253, 0.2);
        }
</style>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}